[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "EcoSalinity"
authors = [{name = "Swaran", email = "Swaranjitroy08@gmail.com"}]
dynamic = ["version", "description"]
